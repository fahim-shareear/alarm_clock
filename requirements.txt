PySide6         # Qt widgets, QPainter, animations, system tray
ntplib          # NTP time sync
tzdata          # timezone data
pytz            # timezone handling
tzlocal         # reads the OS-configured timezone
requests        # IP geolocation fallback for auto timezone detection
apscheduler     # alarm-firing + resync scheduler
pygame-ce       # audio playback for alarm tones (community fork, better wheel coverage)
pyinstaller     # packaging into a standalone executable